export type UserProfileToken = {
    userName: string;
    email: string;
    accountType: string;
    token: string;
};

export type UserProfile = {
    userName: string;
    email: string;
    accountType: string;
}